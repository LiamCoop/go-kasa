package main

// End goal, have a TUI to control smart devices
// Current goal, create tooling around getting device information, controlling the devices.
// parse commands and decide what functions to run
// discover obviously gets all the data, maybe we can pull that into a local nosql / sqlite file
// if we know the name of a device we should be able to hit it by name.
func main() {
}
